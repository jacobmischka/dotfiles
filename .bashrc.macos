eval "$(/opt/homebrew/bin/brew shellenv)"
eval "$(fnm env)"

[ -f ~/.fzf.bash ] && source ~/.fzf.bash

alias copy="pbcopy"
alias paste="pbpaste"
unalias open
export GPG_TTY=$(tty)

export PAGER=/opt/homebrew/bin/less

export NVIM_GTK_CMD_AS_META=1
